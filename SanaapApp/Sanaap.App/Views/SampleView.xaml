<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Sanaap.App.Controls"
             xmlns:syncfusion="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:sfPopup="clr-namespace:Syncfusion.XForms.PopupLayout;assembly=Syncfusion.SfPopupLayout.XForms"
             xmlns:bitControls="clr-namespace:Bit.CSharpClient.Controls;assembly=Bit.CSharpClient.Controls"
             xmlns:extentions="clr-namespace:Sanaap.App.Extensions"
             xmlns:noda="clr-namespace:NodaTime;assembly=NodaTime"
             x:Name="sampleView"
             FlowDirection="RightToLeft"
             x:Class="Sanaap.App.Views.SampleView">
    <ContentPage.Resources>
        <Style TargetType="Label" x:Key="buttonLabel">
            <Setter Property="FontFamily" Value="{StaticResource Shabnam-Bold-FDFont}"/>
            <Setter Property="VerticalTextAlignment" Value="Center"/>
        </Style>

        <Style TargetType="controls:IconLabel">
            <Setter Property="FontSize" Value="{StaticResource xLargeFontSize}"/>
            <Setter Property="TextColor" Value="{StaticResource labelTextColorDark}"/>
        </Style>
    </ContentPage.Resources>

    <ContentPage.Content>

        <StackLayout Padding="10">
            <Label Style="{StaticResource headerText}" Text="بارگذاری تصاdویر"/>
            <Label Style="{StaticResource subHeaderText}" Text="متنی برای زیر عنوان"/>

            <StackLayout Spacing="15" Padding="15">
                <BoxView HeightRequest="1" Color="{StaticResource textBorderColor}"/>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <controls:IconLabel Type="Regular" Text="&#xf03e;"/>
                    <Label Style="{StaticResource buttonLabel}" Text="انتخاب از گالری"/>
                </StackLayout>

                <BoxView HeightRequest="1" Color="{StaticResource textBorderColor}"/>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <controls:IconLabel Text="&#xf030;" Type="Solid"/>
                    <Label Style="{StaticResource buttonLabel}" Text="باز کردن دوربین"/>
                </StackLayout>
            </StackLayout>

            <StackLayout Orientation="Horizontal" Spacing="0">
                <AbsoluteLayout>
                    <Image AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All" Source="{extentions:Image FileName=pelak2.png}"/>
                    <Label FontFamily="{StaticResource Shabnam-Bold-FDFont}" FontSize="{StaticResource largeFontSize}" VerticalTextAlignment="End" HorizontalOptions="Center" Text="11" AbsoluteLayout.LayoutBounds="0.1,0,0.8,1" AbsoluteLayout.LayoutFlags="All"/>
                </AbsoluteLayout>

                <AbsoluteLayout>
                    <Image AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All" Source="{extentions:Image FileName=pelak1.png}"/>
                    <Label FontFamily="{StaticResource Shabnam-Bold-FDFont}" FontSize="{StaticResource largeFontSize}" VerticalTextAlignment="Center" HorizontalOptions="Center" Text="11 ب 253" AbsoluteLayout.LayoutBounds="0.1,0,0.8,1" AbsoluteLayout.LayoutFlags="All"/>
                </AbsoluteLayout>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
